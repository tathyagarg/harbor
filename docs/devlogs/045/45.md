![Logo](https://github.com/tathyagarg/harbor/raw/main/.github/assets/logo.png?raw)
# Changes
---
- [f170cd5](https://url.jam06452.uk/yf54a0): I added a custom New Tab page that is displayed on the internal scheme `harbor:` and is set as the default New Tab page.
- [251e781](https://url.jam06452.uk/14muxay): I wanted to add back, forward and refresh buttons to the toolbar, so I spent this commit making space for those buttons in the toolbar and declaring offsets for them.
- [1a46593](https://url.jam06452.uk/8kcj5m): Added in the actual buttons to the toolbar and made the forward and back buttons functional. There is a small bug where if I click a link, I can then click the back button infinitely many times. idk why this is happening, but I'm guessing it's something to do with a race condition. I'm calling it a feature not a bug.
- [b2eece2](https://url.jam06452.uk/uf2919): Made the refresh button functional. btw, the refresh button's icon is just the character `R` and not a refresh icon because the font I'm using doesn't have a refresh icon.
- [81b58b6](https://url.jam06452.uk/5f4c47): Added some keybinds: `Cmd + T` for new tab, `Opt + Tab` for switching to the next tab, `Opt + Shift + Tab` for switching to the previous tab.
- [e2f3a22](https://url.jam06452.uk/1mj7x61): I wanted to make pages for errors so the browser doesn't just crash when something goes wrong. In doing that I tried to make a global stylesheet for all my builtin pages, but it didn't work because I didn't implement relative URLs (hmm if only i saw this coming). So I got relative URLs working and made `!important` be respected.
- [69fcf94](https://url.jam06452.uk/1pmqx13): Styles were being resolved weirdly under the new refactor, so I fixed that by just resolving everything once more after the declarations are initially handled and use specificity rules.

---
# Next Steps
---
1. Add more builtin pages
2. Track history and make a page for that
